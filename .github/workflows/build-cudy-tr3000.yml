name: Build OpenWrt Plugin (Cudy TR3000)

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: write

jobs:
  build:
    name: Build for Cudy TR3000
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup environment
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libncurses5-dev libncursesw5-dev zlib1g-dev gawk git gettext libssl-dev xsltproc wget unzip python3
        sudo apt-get install -y flex bison texinfo

    - name: Clone OpenWrt source with shallow clone
      run: |
        git clone --depth 1 --branch main https://github.com/openwrt/openwrt.git
        cd openwrt
        git checkout main

    - name: Cache downloads
      uses: actions/cache@v4
      with:
        path: openwrt/dl
        key: ${{ runner.os }}-downloads-cudy-tr3000-${{ hashFiles('openwrt/include/target.mk') }}
        restore-keys: |
          ${{ runner.os }}-downloads-cudy-tr3000-

    - name: Cache build directory
      uses: actions/cache@v4
      with:
        path: openwrt/build_dir/target-*
        key: ${{ runner.os }}-build-cudy-tr3000-${{ hashFiles('openwrt/include/target.mk') }}
        restore-keys: |
          ${{ runner.os }}-build-cudy-tr3000-

    - name: Cache feeds directory
      uses: actions/cache@v4
      with:
        path: openwrt/feeds
        key: ${{ runner.os }}-feeds-cudy-tr3000-${{ hashFiles('openwrt/include/target.mk') }}
        restore-keys: |
          ${{ runner.os }}-feeds-cudy-tr3000-

    - name: Download prebuilt toolchain
      run: |
        cd openwrt
        # 下载适合 mediatek mt798x 的预编译 toolchain
        wget https://downloads.openwrt.org/releases/23.05.0/targets/mediatek/mt798x/openwrt-sdk-23.05.0-mediatek-mt798x_gcc-12.3.0_musl.Linux-x86_64.tar.xz
        tar -xJf openwrt-sdk-23.05.0-mediatek-mt798x_gcc-12.3.0_musl.Linux-x86_64.tar.xz
        # 将 toolchain 复制到指定位置
        cp -r openwrt-sdk-23.05.0-mediatek-mt798x_gcc-12.3.0_musl.Linux-x86_64/staging_dir/toolchain-* staging_dir/

    - name: Add plugin to OpenWrt
      run: |
        cd openwrt
        mkdir -p package/luci-app-easystart
        cp -r ../luci-app-easystart/* package/luci-app-easystart/

    - name: Update feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Configure build
      run: |
        cd openwrt
        echo "CONFIG_TARGET_mediatek=y" > .config
        echo "CONFIG_TARGET_mediatek_mt798x=y" >> .config
        echo "CONFIG_TARGET_mediatek_mt798x_DEVICE_cudy-tr3000-v1=y" >> .config
        echo "CONFIG_PACKAGE_luci-app-easystart=y" >> .config
        make defconfig

    - name: Build plugin
      run: |
        cd openwrt
        make package/luci-app-easystart/compile -j$(nproc) V=s

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: luci-app-easystart-cudy-tr3000
        path: openwrt/bin/packages/*/luci-app-easystart*.ipk

    - name: List built packages
      run: |
        cd openwrt
        find bin/packages -name "luci-app-easystart*.ipk"

    - name: Create release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.run_id }}-cudy-tr3000
        name: Release ${{ github.run_id }} (Cudy TR3000)
        body: |
          自动构建发布
          - 构建时间: ${{ github.run_started_at }}
          - Cudy TR3000 架构
        files: |
          openwrt/bin/packages/**/luci-app-easystart*.ipk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
